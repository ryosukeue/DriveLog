# Project Rules

DriveLog is an iPhone-only portrait SwiftUI app using SwiftData. The Mac local SSD is the primary working tree. Windows edits this repository through VS Code Remote-SSH.

Keep implementation scoped to the active issue. Prefer small, reviewable changes and verify with repository scripts before reporting completion.
